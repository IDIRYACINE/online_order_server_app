import { Callbacks } from '../api/ApiConfig';
declare function synchroniseDatabase(callbacks: Callbacks): void;
export declare const DatabaseSynchroniser: {
    synchroniseDatabase: typeof synchroniseDatabase;
};
export {};
